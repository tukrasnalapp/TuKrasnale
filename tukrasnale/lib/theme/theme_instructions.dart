// Add this import to your existing imports in main.dart
import 'theme/app_theme.dart';

// Then update your MaterialApp widget to use:
// theme: TuKrasnaleTheme.lightTheme,

// Example:
/*
class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'TuKrasnale',
      theme: TuKrasnaleTheme.lightTheme,  // Add this line
      home: // your home widget
    );
  }
}
*/